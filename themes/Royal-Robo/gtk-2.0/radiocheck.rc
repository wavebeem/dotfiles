# vim: set syn=gtkrc:

style "radiocheck" = "button" {
    base[NORMAL]   = shade(1.05, @base_color)
    base[PRELIGHT] = shade(1.15, @base_color)
    bg[SELECTED]   = shade(1.00, @base_color)
    bg[PRELIGHT]   = shade(1.04, @bg_color)

    text[SELECTED] = @fg_color

    engine "murrine" {
        contrast = 0.75
    }
}
